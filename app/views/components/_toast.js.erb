bulmaToast.toast({
    message: "<%= message %>",
    type: "<%= type %>",
    position: "top-center",
    animate: { in: 'fadeIn', out: 'fadeOut' },
    duration: 3000
});

<!-- Required so that, in the case of AJAX calls, the toast doesn't persist. -->
<% flash[:notice] = nil %>
<% flash[:notice_class] = nil %>
<% flash[:notice_subtitle] = nil %>